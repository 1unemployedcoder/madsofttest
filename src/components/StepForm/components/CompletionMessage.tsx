import React from 'react';
import { Typography } from 'antd';

const CompletionMessage: React.FC = () => (
    <Typography.Title level={2}>Спасибо за прохождение теста!</Typography.Title>
);

export default CompletionMessage;
